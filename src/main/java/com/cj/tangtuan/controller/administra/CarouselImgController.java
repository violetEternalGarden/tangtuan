package com.cj.tangtuan.controller.administra;

/**
 * 首页轮播图管理
 */
public class CarouselImgController {


    /**
     * 分类、分页 列表查询所有的轮播图（正在使用、已废弃）
     * 参数：
     * 返回：轮播图展示页面
     */
    public Object findAllCarouselImg(){
        return 0;
    }

    /**
     * 替换轮播图
     * 参数：新的轮播图
     * 返回：是否成功
     */
    public Object addCarouselImg(){
        return 0;
    }

    /**
     * 查询轮播图详情
     * 参数：轮播图ID
     * 返回：轮播图详情页面
     */
    public Object findCarouselImg(){
        return 0;
    }

    /**
     * 修改正在使用的轮播图信息
     * 参数：轮播图信息
     * 返回：是否成功
     */
    public Object updCarouselImg(){
        return 0;
    }
}
